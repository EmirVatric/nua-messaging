<div class="card my-3 ">
  <h5 class="card-header">
    <% if @message.outbox.user.is_doctor? %>Dr.<% end %>
    <%= @message.outbox.user.full_name %>
  </h5>
  <div class="card-body">
    <p class="card-text"><%= @message.body %></p>
    <%= link_to "Reply", new_message_path(id: @message.id), class: "btn btn-primary" %>
    <%= link_to "I've lost my script, please issue a new one at a charge of €10", lost_script_path, class:"btn btn-primary" if @current_user.is_patient%>
  </div>
</div>